import 'package:flutter/material.dart';

class VideoConsult extends StatelessWidget {
  const VideoConsult({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Container(
      child: Column(
        children: [Text("Hello , How can i help you!.")],
      ),
    ));
  }
}
